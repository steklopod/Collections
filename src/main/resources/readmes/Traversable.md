## Типаж Traversable

В верхней части иерархии коллекций в Скала находится типаж `Traversable`. Его единственная абстрактная операция - `foreach`:

```scala
    def foreach[U](f: Elem => U)
```
![alt text](https://github.com/steklopod/Collections/blob/master/src/main/resources/images/collections.png "collections")

Классы коллекции, которые реализуют `Traversable`, просто должны определить этот метод; все другие методы могут быть 
унаследованы от `Traversable`.

Метод `foreach` предназначен для "обхода" всех элементов коллекции и применения фунции `f` к каждому элементу. 
_Тип операции_: `Elem => U`, где `Elem` - тип элементов коллекции, а `U` - произвольный тип результата. Вызов `f`
 выполняется только для его побочного эффекта; на самом деле любой результат функции `f` игнорируется.

`Traversable` также определяет многие конкретные методы, которые относятся к следующим категориям:

* **Сложение**: `xs ++ ys` - возращает коллекцию из элементов коллекций `xs` и `ys`.

* **Преобразование**: `map`, `flatMap` и `collect`, которые производят новую коллекцию, применяя некоторую функцию `f` к элементам коллекции.

* **Конверсия**: `toArray`, `toList`, `toIterable`, `toSeq`, `toIndexedSeq`, `toStream`, `toSet`, `toMap`, 
которые превращают коллекцию `Traversable` в нечто более конкретное.

* **Копирование**: `copyToBuffer` и `copyToArray`, элементы коллекции копируются в буфер или массив соответственно.

* **Информация о размере**: `isEmpty`, `nonEmpty`, `size` и `hasDefiniteSize`: 
Traversable-коллекции могут быть конечными или бесконечными. Например поток натуральных чисел `Stream.from(0)`. 
Метод `hasDefiniteSize` указывает, может ли коллекция быть бесконечной и возвращает `true`, если коллекция конечна.

* **Поиска элементов**: `head`, `last`, `headOption`, `lastOption` и `find`. Они выбирают первый или последний элемент 
коллекции, а также первый элемент, соответствующий условию. Обратите внимание, что не все коллекции имеют четко
 определенный смысл того, что означает `первый` и `последний`. _Например, хэш-набор может хранить элементы в 
 соответствии с их хэш-ключами, которые могут меняться от запуска к запуску. В этом случае «первый» элемент 
 хеш-набора также может быть различным для каждого запуска программы._ Коллекция упорядочена, если она всегда возвращает свои 
 элементы в том же порядке. Большинство коллекций упорядочены, но некоторые (например, хэш-наборы) не гарантируют 
 порядок. Коллекции Scala предоставляют упорядоченные альтернативы для всех типов коллекций.
  _Например, упорядоченная альтернатива для HashSet - LinkedHashSet._

* **Поиска элементов в подколлекциях**: `tail`, `init`, `slice`, `take`, `drop`, `takeWhile`, `dropWhile`, `filter`, `filterNot`, `withFilter`. 
Все они возвращают некоторую подколлекцию, идентифицированную диапазоном индексов или некоторым предикатом.

* **Операции разбиения**: `splitAt`, `span`, `partition`, `groupBy`, которые разбивают элементы этой коллекции на несколько подкатегорий.

* **Проверка элементов**: `exists`, `forall`, подсчитывают, какие элементы коллекции тестов имеют заданный предикат.

* **Свертка**: `foldLeft`, `foldRight`, `/:`, `:\`, `reduceLeft`, `reduceRight`, которые применяют двоичную операцию 
к последовательным элементам.

* **Специфичная свертка**: `sum`, `product`, `min`, `max`, которые работают с коллекциями определенных типов (числовые или сопоставимые).

* **Строковые операции**: `mkString`, `addString`, `stringPrefix`, которые предоставляют альтернативные способы преобразования коллекции в строку.

* **Представление (View)**: состоящих из двух перегруженных вариантов метода `view`. `View` - это коллекция, которая инициализируется
 лениво.

_Если этот проект окажется полезным тебе - нажми на кнопочку **`★`** в правом верхнем углу._


Метод                     | Что делает          
------------------------- | --------------------
**Абстрактный метод**     |
`xs foreach f`            | Выполняет функцию `f` для каждого элемента `xs`
**Сложение**              | 
`xs ++ ys`                | Коллекция, состоящая из элементов как `xs`, так и `ys`. `ys` представляет собой коллекцию `TraversableOnce`, то есть либо `Traversable`, либо `Iterator`.
**Отображения**          |
`xs map f`                | Коллекция, полученная от применения функции `f` к каждому элементу из `xs`.
`xs flatMap f`            | Передает все элементы коллекции функции `f`, которая в свою очередь возвращает коллекцию, и конкатенирует результаты
`xs collect f`            | Коллекция, полученная от применения частичной функции `f` к каждому элементу из `xs`, для которого он определен и собирающий результаты.
**Конверсии**             |
`xs.toArray`              | Преобразует коллекцию в массив.
`xs.toList`               |
`xs.toIterable`           | 
`xs.toSeq`                | 
`xs.toIndexedSeq`         | 
`xs.toStream`             | 
`xs.toSet`                | 
`xs.toMap`                | Преобразует коллекцию пар ключ / значение в Map. Если в коллекции нет пар в качестве элементов, вызов этой операции приводит к ошибке статического типа.
**Копирование**             |
`xs copyToBuffer buf`       | Копирует все элементы коллекции в буфер `buf`
`xs copyToArray(arr, s, n)` | Копирует не более `n` элементов коллекции в массив `arr`, начинающийся с индекса `s`. _Последние два аргумента являются необязательными._
`xs copyToBuffer buf`       | Копирует все элементы коллекции в буфер `buf`
**Операции с информацией о размере** |
`xs.isEmpty`                         | Проверяет, пуста ли коллекция.
`xs.nonEmpty`                        | Проверяет, содержит ли коллекция элементы.
`xs.size`                            | Кол-во элементов в коллекции.
`xs.hasDefiniteSize`                 | `true` если коллекция имеет конечный размер.
**Поиск элементов**  |
`xs.head`            | Первый элемент коллекции (или некоторый элемент, если не задан порядок).
`xs.headOption`      | Первый элемент `xs` в Option-типе или `None`, если `xs` пуст.
`xs.last`            | Последний элемент коллекции (или некоторый элемент, если не задан порядок).
`xs.lastOption`      | Последний элемент `xs` в Option-типе или `None`, если `xs` пуст.
`xs find p`          | Option-тип, содержащая первый элемент в `xs`, который удовлетворяет предикату `p`, или `None`, если ни один элемент не подходит.
**Подколлекции**     |
`xs.tail`            | Остальная часть коллекции, кроме `xs.head`.
`xs.init`            | Остальная часть коллекции, кроме `xs.last`.
`xs slice (from, to)`| Коллекция, состоящая из элементов в некотором диапазоне индексов `xs` (от from и до to не включая).
`xs take n`          | Коллекция, состоящая из первых `n` элементов `xs` (или некоторых произвольных n элементов, если не определен порядок).
`xs drop n`          | Остальная часть коллекции, кроме `xs`, принимает `n`.
`xs takeWhile p`     | Самый длинный префикс элементов в коллекции, которые удовлетворяют `p`.
`xs dropWhile p`     | Коллекция без самого длинного префикса элементов, удовлетворяющих всем требованиям.
`xs filter p`        | Коллекция, состоящая из тех элементов `xs`, которые удовлетворяют предикату `p`.
`xs withFilter p`    | Нестрогий фильтр коллекции. Последующие вызовы `map`, `flatMap`, `foreach` и `withFilter` будут применяться только к тем элементам `xs`, для которых условие `p` истинно.
`xs filterNot p`     | Коллекция, состоящая из тех элементов `xs`, которые не удовлетворяют предикату `p`.
**Разделение**       |
`xs splitAt n`       | Разделяет `xs` в позиции, создав пару коллекций `(xs take n, xs drop n)`.
`xs span p`          | Разделяет `xs` в соответствии с предикатом, предоставляя пару коллекций `(xs takeWhile p, xs.dropWhile p)`.
`xs partition p`     | Разделяет `xs` на пару коллекций; один с элементами, которые удовлетворяют предикату `p`, а другой - с элементами, которые этого не делают, давая пару коллекций `(xs filter p, xs.filterNot p)`.
`xs groupBy f`       | Разделяет `xs` на отображение наборов в соответствии с дискриминаторной функцией `f`.
**Проверка элементов**|
`xs forall p`         | Логическое значение указывает, удовлетворяет ли предикат `p` для всех элементов `xs`.
`xs exists p`         | Логическое значение указывает, довлетворяет ли предикат  `p` для некоторого элемента в `xs`.
`xs count  p`          | Число элементов в `xs`, удовлетворяющих предикату `p`.
**Свертка**            |
`(z /: xs)(op)`        | Применяет двоичную операцию `op` между последовательными элементами `xs`, идущими слева направо и начиная с `z`.
`(xs :\ z)(op)`        | Применяет двоичную операцию `op` между последовательными элементами `xs`, идущими справа налево и начиная с `z`.
`xs.foldLeft(z)(op)`   | То же что и `(z /: xs)(op)`.
`xs.foldRight(z)(op)`  | То же что и `(xs :\ z)(op)`.
`xs reduceLeft op`     | Применяет двоичную операцию `op` между последовательными элементами непустого набора `xs`, идущими слева направо.
`xs reduceRight op`    | Применяет двоичную операцию `op` между последовательными элементами непустого набора `xs`, идущими справа налево.
**Специфичная свертка**|
`xs.sum`               | Сумма значений числовых элементов коллекции `xs`.
`xs.product`           | Произведение значений числовых элементов коллекции `xs`.
`xs.min`               | Минимум значений упорядоченных элементов коллекции `xs`.
`xs.max`               | Максимум значений упорядоченных элементов коллекции `xs`.
**Строка**             |
`xs addString (b, start, sep, end)`| Добавляет строку в `StringBuilder b`, которая показывает все элементы `xs` между разделителями `sep`, заключенными в `start` и `end`. `start`, `sep`, `end` являются необязательными.
`xs mkString (start, sep, end)`    | Преобразует коллекцию в строку, которая отображает все элементы `xs` между разделителями `sep`, заключенными в `start` и `end`. `start`, `sep`, `end` являются необязательными.
`xs.stringPrefix`                  | Имя коллекции в начале строки, возвращаемой из `xs.toString`.
**Представление (View)**|
`xs.xs.view`            |Производит представление по `xs`.
`xs view (from, to)`    |Производит представление, представляющее элементы в некотором диапазоне индексов `xs`.

_Если этот проект окажется полезным тебе - нажми на кнопочку **`★`** в правом верхнем углу._

>дополнительная шпаргалка:
![alt text](https://github.com/steklopod/Collections/blob/master/src/main/resources/images/collection_methods_1.png "collection_methods_1")

![alt text](https://github.com/steklopod/Collections/blob/master/src/main/resources/images/collection_methods_2.png "collection_methods_2")

_Если этот проект окажется полезным тебе - нажми на кнопочку **`★`** в правом верхнем углу._

* [=> Типаж Iterable](https://github.com/steklopod/Collections/blob/master/src/main/resources/readmes/Iterable.md)

[<= содержание](https://github.com/steklopod/Collections/blob/master/readme.md)

